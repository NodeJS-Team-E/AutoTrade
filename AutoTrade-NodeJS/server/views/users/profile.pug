extends ../_layout
block content
    div(class="row")
        div(class="col s6 m4")
            div(class="card large")
                div(class="card-image")
                    img(src=user.pictureUrl)
                    span(class="card-title")=user.username
                div(class="card-content")
                    small Email  
                    small=user.email
                    p Don't like the profile picture? Change it!
                        a(href="/update-profile") Settings
                    //- form(action="post" name="update-picture" id="update-picture")
                    //-     div(class="file-field input-field")
                    //-         div(class="waves-effect waves-light btn")
                    //-             span Update!
                    //-             input(type="file")
                    //-         div(class="file-path-wrapper")
                    //-             input(class="file-path validate" type="text")

                       
                    if user.adverts
                        each advert in user.adverts
                            strong=advert.title
                            strong=advert.description
                    a(href="/logout")  Logout